FAÇA no terminal antes de subir o docker
mkdir -p /data/web/static /data/web/media
chown -R 1000:1000 /data/web
chmod -R 755 /data/web


docker ps -a # lista todos docker
docker images ls # lista imagem
docker compose up --build # sempre que inciar o docker
docker rm -f __nome_do_docker__
docker system prune -a
docker compose down -v

docker compose up --build
docker compose up --build --remove-orphans --renew-anon-volumes --force-recreate

docker compose up -d
docker compose down

docker compose run --rm djangoapp ___comando__python -V
docker compose run --rm djangoapp /bin/sh -c 'echo $PATH $ALLOWED_HOSTS'

docker exec -it djangoapp /bin/sh

OBS: Se não conseguir criar o app, der esse erro:
CommandError: [Errno 13] Permission denied: '/djangoapp/blog'

docker compose run --rm djangoapp sh -c "whoami && id && pwd && ls -ld /djangoapp"
duser
uid=1000(duser) gid=1000(duser) groups=1000(duser)
/djangoapp
drwxrwxr-x    3 1002     1002          4096 Jun 16 17:19 /djangoapp

Problema: 
duser = uid 1000
/djangoapp = dono 1002:1002

Resolver:
sudo chown -R 1000:1000 djangoapp
